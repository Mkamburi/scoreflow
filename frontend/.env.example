# ScoreFlow frontend environment variables

# Flat.io Embed App ID — create at https://flat.io/developers/apps
# Works on localhost without domain whitelist during development.
VITE_FLAT_APP_ID=

# Optional: override API base URL (defaults to Vite dev proxy at /api)
# VITE_API_BASE_URL=http://127.0.0.1:8000
